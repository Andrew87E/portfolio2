import { Fira_Code, Indie_Flower, Glory, Arizonia } from "next/font/google";

export const firaCode = Fira_Code({
    display: "swap",
    subsets: ["latin"],
});


export const indieFlower = Indie_Flower({
    display: "swap",
    subsets: ["latin"],
    weight: ['400'],
});

export const glory = Glory({
    display: "swap",
    subsets: ["latin"],
});

export const arizonia = Arizonia({
    display: "swap",
    subsets: ["latin"],
    weight: ['400'],
    preload: true,
});
